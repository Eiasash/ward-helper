## CHANGELOG

- **2026-06-23 (REHAB_NOTES.md conditional-load gate)**: Added a LOAD GATE to WORKFLOW step 2 — REHAB_NOTES.md (~48KB) is read iff the note type is rehab admission/daily round/discharge, and explicitly not read for plain admission/discharge/consult/ED. Previously the only load instruction was ~160 lines deep and one-directional with no negative gate. Step 2 also now lists the three rehab note types. Sets up the ~12k-token non-rehab saving, realized once the loader.ts runtime gate lands (tracked follow-up); the prompt gate's immediate effect is preventing rehab-template bleed-in. No change to note output format.
- **2026-06-19 (lane reconciliation — repo/live drift union)**: Reconciled a bidirectional drift between the repo copy and the live working copy of this skill. Backported from the live lane: the **Rehab discharge (סיכום אשפוז שיקומי)** section + complex-medical checklist (`REHAB_NOTES.md`), the **Word choice — clinical terminology** rule, and the **raw-lab-values-only** rule. Preserved from the repo lane: the **PRE-DELIVERY SELF-CHECK — SKELETON SECTIONS ARE NOT DELIVERY** gate + its WORKFLOW step (a pre-export skeleton/placeholder scan). Neither lane had the other's content — union of both, nothing dropped. Also completes the earlier extraction of REHAB NOTES + CHANGELOG to sibling files.
- **2026-06-16 (rehab admission — discussion-capsule + ortho-HPI framing + contamination guard, PATIENT calibration)**: Calibrated on the PATIENT 16/06 case (85F, post-CRIF/Gamma-nail L intertrochanteric femur fracture, live admission-in-progress screens). Encoded deltas to the **rehab admission** section + checklist: (1) **דיון now OPENS with a one-paragraph synthesis CAPSULE before any `#`** — age + independent/previously-walking baseline + `רקע כמפורט מעלה` + admitted to [surgical dept] for surgery and transferred to our ward for rehab + on-arrival exam (pertinent findings ONLY) + pertinent latest imaging/labs, then `כעת מציגה את הבעיות הבאות לדיון:` + the `#` list. NOT a bare `#` list. (2) **Each `#` EXPANDS the reasoning**, not just a label — name loading doses already given (Vit-D 50,000u), the drug-agent choice and *why* (TERIPARATIDE/FORTEO anabolic for bisphosphonate **treatment failure** = a fracture under/after a bisphosphonate), the renal constraint (CrCl). (3) **מחלה נוכחית "as if admitted to Ortho"** = write the source surgical dept's admission HPI (fall→fracture→fixation→post-op course incl transfusion/control-film/Vit-D→transfer), not the at-home story; home/social → הצגת + תפקוד. (4) **Do NOT propagate a source's "ללא רקע רפואי"** when the coded background contradicts (coded list is usually richer than the acute discharge — GERD, spinal stenosis, prior vertebral fx s/p kyphoplasty, prior surgeries, prior osteoporosis Rx); trust coded, flag the discrepancy. (5) **תוכנית** now must carry drug-start/stop decisions *with timing* + an **estimated suture/staple removal date by POD** (hip/proximal-extremity POD 10–14 per ortho-reference §6; compute from surgery date). (6) **NEW critical rule — contamination guard:** the בדיקות עזר free-text panel can hold wrong-patient/template values (PATIENT: Na133/BUN73/Cr2.76/Trop264/CRP21.44 foreign to a patient whose real Cr≤1.01/Trop<10/CRP0.12) — cross-check every value, flag + clear, never paste. (7) **Home floor/elevator is discharge-critical and must be reconciled across admission vs social-work** (PATIENT: admission `קומה 3 ללא מעלית` vs SW "has elevator"). (8) **New general formatting rule (all note types) — WORD CHOICE:** prefer real medical terminology / inline English clinical terms over literal Hebrew calques that read AI-generated (שבר פתולוגי not שבר שבריריות; GERD; kyphoplasty; normocytic; neurogenic claudication; lumbar spinal stenosis; treatment failure); complementary to the JARGON rule (which targets specialist abbreviations toward lay readers). No change to acute-admit / discharge / consult output skeletons.
- **2026-06-10 (rehab admission — template overhaul, PATIENT + PATIENT calibration)**: Reworked the **rehab admission** section and its checklist against two filed cases (PATIENT post-AVR+MV-repair, PATIENT post-BKA) plus Eias directives. Encoded deltas: (1) **בדיקות עזר and בדיקות מעבדה are now dedicated sections** (Eias preference) — imaging/ECG/Doppler get their own one-line-each block and labs their own raw-value prose block; do NOT fold imaging into the discussion the way some filed doctor notes do; **no specimen/accession reference numbers** anywhere in labs. (2) **דיון** is the `#` problem list with NO plan inside; **תוכנית** is a SEPARATE final paragraph, ONE line per item. (3) **אבחנות פעילות kept minimal** = `ADMISSION FOR REHABILITATION` + active acute only (e.g. UTI); the recent surgery → ניתוחים בעבר, resolved/chronic → ברקע; **pull every dx from the EMR CODED list, not the narrative prose** (filed coded `ADENOCARCINOMA OF RECTUM` even when HPI says SCC-anus — use coded, flag mismatch). (4) **The rehab stay gets a NEW אשפוז number** — never carry the acute-stay number. (5) **Vitals = the admission-encounter bedside set, NOT the AZMA header snapshot** (they diverge; header is a stale morning reading). (6) **FILL ALL GAPS** — full תפקוד grid from PT (9 ADL 3-tier + מגורים/עזרה/ניידות-aid/התמצאות/הזנה), floor/elevator + room/bed resolved; genuine unknowns/contradictions live ONLY in the non-copy team box. (7) **Always output the medication orders to give/enter in the same turn as the note** (grouped קבוע/PRN/held); default = the sending-department AZMA active orders unless the transfer letter overrides; **implement documented med recommendations** (add/reduce/stop/titrate), not just copy the active list; verify Israeli brand names first. Quality checklist rewritten to match. No change to acute-admit / discharge / consult note types.

- **2026-06-08 (rehab discharge — complex-medical checklist)**: Added a compact **Complex-medical rehab discharge checklist** to the rehab-discharge section, calibrated on the the ESRD/HD case case (75M, ESRD on HD 3×/wk, post-CRIF intertrochanteric L femur, DM, epilepsy, PMR on MTX, G6PD). Eight pre-signing lines, each a real miss on the case: dialysis status/unit/schedule/access + fistula protection + HD-unit EPO/iron is the first question; renal drug dosing reconciled against renal-adjusted not home doses (Levetiracetam 1000→500, ACE dropped for hyperK, no metformin); CKD-MBD ≠ osteoporosis (PTH/Ca/Phos/VitD, active-D + binder + Ca, no bisphosphonate, denosumab→hypocalcemia); HbA1c unreliable in CKD/anemia/EPO/transfusion (titrate to glucometer); G6PD card + trigger-drug avoidance; immunosuppressants (MTX) in ESRD — state held/resumed + who follows; rising CRP/WBC + pending cultures not signed off without a source; signature = senior/case-manager. the ESRD/HD case is now the complex-medical exemplar alongside the deconditioning exemplar (deconditioning) and the femur exemplar (clean ortho). No change to note output format.
- **2026-06-08 (rehab discharge — condensation + ortho-rehab calibration)**: Same-day follow-up to the rehab-discharge section, calibrated on the the femur exemplar femur case (post-CRIF L femur + L1/L3 compression fractures). Added **Delta 7 condensation defaults** per Eias "condense relevant sections": מחלה נוכחית ≤2 paragraphs; the מהלך ודיון capsule must NOT re-tell מחלה נוכחית (1–2 sentences then straight to functional baseline — redundancy between the two is the main bloat source); בדיקות עזר imaging keeps only management-changing findings and drops the level-by-level degenerative disc readout; בדיקות מעבדה essentials only (drop normal coags). Added **ortho-rehab specifics**: active dx = ADMISSION FOR REHABILITATION + STATUS POST [procedure] (not DECONDITIONING, which is the medical/ICU-decline case); ניתוחים באשפוז = rehab-stay procedures only (index surgery goes in מחלה נוכחית); typical בעיות אחרות set. Added geri-analysis pearls (chat, not note): osteoporosis is treatment-defining after a fragility fracture — push real treatment not boilerplate follow-up, especially with a new in-admission vertebral fracture; opioid taper from the acute discharge's standing oxycodone in a pain-free mobilizing patient; reconcile the discharge drug list against *current rehab orders* not the acute discharge (CCB Amlodipine-vs-Lercanidipine divergence); dual antihypertensive + presyncope → orthostatic check. the femur exemplar is now the cleaner ortho-rehab exemplar alongside the deconditioning exemplar's deconditioning case.
- **2026-06-08 (rehab discharge — closes the line-657 TBD)**: Added **Rehab discharge (סיכום אשפוז שיקומי)** section, calibrated on the the index case 08/06/26 printed discharge (post-pancreatectomy deconditioning, CRKP carrier). Resolves the long-standing "rehab discharge format = TBD, send an example" placeholder. Key encoded deltas vs the general/internal discharge: (1) אבחנות פעילות leads with ADMISSION FOR REHABILITATION + DECONDITIONING + still-active isolation/carrier status (repeated in body with until-date); (2) מחלה נוכחית = inherited acute story (audited), not the rehab course; (3) **מהלך ודיון has NO `#` headers** — instead: rehab capsule opener → functional-baseline paragraph (BADL/IADL + מט"ב hours) → the **מצב תפקודי fill-in template** (verbatim skeleton with MMSE/clock + the per-transfer 3-tier grades + the two FUNCTIONAL INDEPENDENCE MEASURE lines, motor home/admission/discharge + cognitive /35) → **בעיות אחרות** as `*`-bullets tying event→action with drug changes inline → לסיכום with honest discharge-timing reason; (4) recs = home PT (יט"ב) + OT home visit; (5) המשך תרופתי carries `(למשך N חודשים)` and must reconcile against the בעיות אחרות dose-change bullets; (6) **signature = senior/case-manager (this print: the rehab case manager ***REDACTED-LICENSE***), NOT the fellow** — do not auto-stamp Eias's lic on rehab discharge. Captured the FIM-trajectory teaching (61→35→63 = deconditioning then recovery to baseline) and the two-patients-in-one-bundle mapping caveat. Numbers originate in the Chameleon אומדנים grids (FIM/BADL, MMSE, Clock-CDT, MoCA), entered as dropdowns and auto-totaled; the prose block distills them; use MMSE *or* MoCA, not both. **FIM→Hebrew grade conversion table added** (7/7 עצמאי / 6/7 השגחה / 5/7 עזרה קלה / 4/7 עזרה קלה לבינונית / 3/7 עזרה בינונית / 2/7 עזרה בינונית למלאה / 1/7 עזרה מלאה) — grade words are the FIM score rendered as Hebrew, never free-handed; documented which מצב תפקודי lines are FIM-backed vs PT-derived. **Gendered M/F templates** both exist — match the patient (the deconditioning exemplar male / the femur exemplar female).
- **2026-06-03 (rehab SOAP — labs to own `מעבדה` section)**: Per Eias directive, labs now go in their **own `מעבדה:` section placed after `O`, before `A`** (date-stamped) — never in `O`. A *decision-driving* value may still be cited inline in the relevant `A` bullet as reasoning (`*כלייתי - החמרה קלה, קראטינין 1.3`); that's interpretation, not the data section. **Reverses the 2026-05-19 "labs inline in A" rule** (which itself reversed the older "labs in O"). `O` now holds bedside exam + ECG/imaging findings (`ממצאי עזר`) only. Updated: FIRST-DAY / STABLE / COMPLEX templates (each gets a `מעבדה` section), COMPLEX rules line, and rehab-rounds checklist. Synced with `rehab-quickref` (same change, same day). Memory #25 already reflects this. No change to admission/discharge/consult note types (they keep their dedicated `בדיקות מעבדה` field).
- **2026-05-19 (rehab SOAP — ward-helper R2/R3 sync)**: SOAP rehab-rounds section synced to ward-helper's Phase 2 prompt behavior (merge `999dc07`). (1) **Verbosity gradient made explicit** — cross-pattern invariant added: `A` always more detailed than `P`; FIRST-DAY is the most verbose SOAP of the admission, subsequent rounds lean delta. (2) **P as delta, not reprint** — COMPLEX-pattern `P לביצוע` reframed as a merge into the carried-forward plan (lead with נוסף/הופסק/שונה, collapse unchanged continuations to one line), with a carve-out so genuinely active issues still get specific action lines. (3) **Labs moved O→A** — lab/imaging values now go inline in the relevant `A` problem bullet only when decision-driving, never in `O` (`O` = bedside exam only); reverses the prior "labs inline in O" rule and fixes a chat-vs-ward-helper divergence. COMPLEX example, FIRST-DAY template, COMPLEX rules, and the rehab-rounds checklist all updated. (4) **A bullets framed as trajectory** vs the prior round (unchanged/changed/resolved/new), matching ward-helper's continuity path. No change to admission/discharge/consult note types. *Note: labs-placement portion superseded 2026-06-03 — labs now in own `מעבדה` section.*
- **2026-05-18**: Integration with the `azma-ui` skill (drift control). The AZMA EMR INTERPRETATION section hands off screenshot *decoding* (med-grid icons, `ניהול מחלקה` census columns, status icons, cell colours) to `azma-ui` — the single source of truth — and scopes itself to *interpretation* for note-writing. This skill must not restate AZMA icon/column/colour legends. No change to note output.
- **2026-05-04 (rehab v2)**: Iterative refinement after PATIENT / PATIENT / PATIENT / PATIENT case calibration on the same day. **Major changes**: (1) **Three-pattern model** replaces two-pattern: FIRST-DAY (detailed regardless of complexity, capsule mandatory) / STABLE (subsequent, no acute issues) / COMPLEX (subsequent, ≥2 triggers). The day-1 SOAP is its own thing — it establishes the chart for the next month. Subsequent rounds drop the capsule and shrink. (2) **Patient capsule formula** added explicitly: `[age] [marital] [parents]. [living]. רקע רפואי כולל [chronic dx]. בבסיס [pre-admission ADL]. כעת לאחר [event] [date]. ביום N לאחר.` Cross-check demographics against admission (PATIENT 13/04 had wrong marital status + parent count — chart corruption). (3) **Goal-by-indication table** replaces the lazy `מוקדם מדי` default: post-hip/knee → `עצמאות בניידות ובשרותים`, post-spine → `עצמאות במעברים והליכה עם עזרים`, pre-cardiac → `חיזוק תפקודי לקראת ניתוח [date]`, post-deconditioning → `חזרה לתפקוד בסיסי`. `מוקדם מדי` legitimate only for CVA days 0-3. (4) **Focused exam by indication** added to FIRST-DAY: CVA→neuro+gait, hip/knee→wound+per-limb, spine→wound+motor+sensation+sphincter, pre-cardiac→murmur+JVP+edema, ESKD→fistula. (5) **Rehab clinical pearls** section added with 10 high-impact gotchas: source-dept claim audit (PATIENT's "Vit D 20 maintenance" was wrong), CKD-MBD vs osteoporosis bisphosphonate trap (PATIENT's "Aclasta in discharge" was unsafe in HD), demographics cross-check, dizziness during PT as clinical event not anecdote, pre-admission falls + in-rehab near-falls as connected syndrome, iatrogenically deprescribed SSRIs in HD, PT communication after pre-syncope, aspirin without CAD indication in fall-risk patient, polypharmacy as questions not directives, day-1 social work for high-floor-alone patients. (6) Quality checklist for rehab rounds expanded with three-pattern triage + 10 new items.
- **2026-05-04 (rehab v1)**: **Rehab notes extension** added (PATIENT + PATIENT cases, 2026-04 transfer-to-rehab pattern). New section **REHAB NOTES — admission and daily rounds** covering: (1) **rehab-admission** mode — inheritance pattern from source dept (neurology/orthopedics/CTS/geri-mugbar), explicit do-not-retake-history rule, on-arrival snapshot as the only fresh content. (2) **rehab-rounds** mode — Pattern A (synthesis) vs Pattern B (problem-list `*` bullets with domain prefixes: אורתופדית/זיהומית/תפקודית/כלייתי/לבבי/לחץ דם/בצקת/נשימתי/נוירולוגית/כאב/שתן/מטבולית/פצע/עצירות/שינה/תזונתי/פסיכולוגית) selected by complexity triggers (IV ABX with levels, post-op wound, active diuresis, BP/glucose titration, AKI, pending procedure). (3) **תוכנית טיפול goal anti-pattern** flagged — institutional norm of leaving the field as `[case header]. מוקדם מדי לקבוע מטרה.` from day 0 to day 18+ is template inertia, not a goal. Should update post team meeting (~day 5–7) to a SMART target with destination + functional milestones + timeline. Flag in geriatric analysis if frozen past day 7. (4) **Allied health intake → doctor's note (read-only)** — PT/OT/dietitian/SLT intakes are the source of truth for pre-admission ADL/IADL, MOCA/MMSE, caloric targets, per-limb functional grading; doctor references but does not recreate. (5) Quality checklist updated with rehab-admission + rehab-rounds subsections. Cadence: daily M–F per patient regardless of complexity (Eias rehab rotation 2026-05). Frontmatter triggers expanded: "כתוב לי קבלת שיקום", "ביקור רופא", "rehab round", "daily round". *Note: superseded by v2 same-day — three-pattern model with FIRST-DAY pattern explicit.*
- **2026-04-29**: PATIENT case calibration. **Major changes**: (1) **Admission תפקוד subsection format** specified explicitly — structured key:value lines with **3-tier ADL grading** (עצמאי / עזרה חלקית / עזרה מלאה) for each of הלבשה, רחצה, אכילה, הכנת אוכל, ניידות, ניידות בכ"ג, מעברים, שליטה על שתן, שליטה על יציאה. Plus: מגורים, עזרה, התמצאות, הזנה. (2) **NO MRS (Modified Rankin Scale) in admission templates** — it's not part of the standard SZMC admission ADL section. Use plain Hebrew (`סיעודי`, `מרותק למיטה`) for narrative severity. (3) New **AZMA EMR INTERPRETATION** section: med grid shows ACTIVE orders only; struck-through = held/discontinued; absent meds may be intentional clinical choices, not omissions. Frame critique as questions, not directives. (4) **Frailty-adjusted dosing** is a defensible decision frame — sub-therapeutic anticoag in bedbound demented patient with PUD/UGI bleed history is not automatically wrong. Apply guideline lens only after asking if frailty changes the risk/benefit. (5) Geriatric analysis section now flags trend-data importance — multi-admission Hb/Plt/BP trajectories are diagnostic in their own right (today's case: Hb 8→11 in 2018-2019 → 15.6 in 2022 → 19.4 in Jan 2026 → 18.7 Apr 2026 = unmasked PV after PUD bleeding resolved). (6) Quality checklist for ward admission notes adds: 3-tier ADL items, NO MRS, trend flagging.
- **2026-04-28**: Print-verified discharge calibration on PATIENT case. **Major changes**: (1) Recommendations + drug list switched from numbered 1-N to **DASH list (-)** — easier to delete items without renumbering, EMR auto-numbers drug list anyway. (2) Active dx narrowed to **acute admit reason ONLY** (chronic conditions all go to ברקע); add `BLOOD TRANSFUSION X N units` if any units given. (3) ניתוחים באשפוז = NGT/urinary cath/PEG events only (skip peripheral IV). (4) מחלה נוכחית: field APPENDS to admission text — provide AUDITED admission paragraph for retro paste-over. (5) **Lab section overhaul**: categorized (ביוכימיה / מדדי דלקת / ספירת דם / גזים), prose trends with NO arrows ('בקבלה X, במהלך Y, בשחרור Z'), MAX 3 numbers/test, drop eGFR+BUN if Cr listed, correct total Ca for albumin same-day (`Cor = Ca + 0.8×(4-Alb)`), strip H/L suffixes use `(מעל/מתחת לנורמה)` parens. (6) בדיקות עזר: keep curated section, strip doctor names/accession numbers/vial IDs. (7) **מהלך ודיון opens with patient summary template** before any `#` ('בת X+הרקע→התקבלה בשל→במיון→מעבדה→ייעוצים→אושפזה→בקבלתה→הציגה הבעיות:'). (8) `# טיפול יעדי` only if documented decision, not speculative. (9) המלצות drop boilerplate ("פנייה למיון/הבא סיכום"). (10) Tx: always add `Water 400ml X3/d לפי צורך` for PEG; borderline home meds → PRN with `לפי החלטת רופא מטפל`, never deprescribe in print. (11) Eias signature: lic 000147224 baked in.
- **2026-04-27**: Section order corrected based on PATIENT + PATIENT finalized discharges. Order is אבחנות פעילות → ניתוחים באשפוז → ברקע → הצגה → תלונה → מחלה נוכחית → רקע (with פרוט+אבחנות בעבר+ניתוחים) → רגישויות → הרגלים → בדיקה → תרופות בבית (auto sidebar) → בדיקות עזר (cultures→imaging, NEVER labs) → מעבדה (typed trends) → דיון → המלצות → תרופתי → auto → חתימה. Drug name casing rule clarified: ALL-CAPS in narrative only, Title Case in auto drug-list sections (don't fight Chameleon DB). Added RECOMMENDATION LENGTH GUARD (≤180 chars per bullet to avoid Chameleon mid-word truncation — verified failure in both finalized notes). Added SGLT2i-pending-cardiology pattern + active-diagnosis completeness check (Type 2 MI in septic shock, new HF phenotype, IDA workup, incidental CT findings).
- **2026-04-20**: Major restructure based on PATIENT 20/04/26 final discharge. New printed-order section sequence (18 steps); רגישויות + תרופות בבית moved before narrative; בדיקות עזר promoted to discrete pre-narrative section; בדיקות מעבדה reduced to prose trends; PT/OT/dietician removed from main body (handled by separate Chameleon sub-tabs and PT auto-attach); המלצות + טיפול תרופתי standardized as numbered lists; glossary section removed; signature ordering corrected; added discharge-specific geriatric red-flag checklist (benzos after delirium, Apixaban dosing, Vit D restart, Mirtazapine reassessment, prophylactic ABX restart logic, ABX-culture narrowing).
