/* Per-app config for shared install-promo.js. Loaded BEFORE install-promo.js.
 * CSP forbids inline scripts (script-src 'self'), so config must be its own file. */
window.PWA_INSTALL_CONFIG = { appName: 'Ward Helper' };
