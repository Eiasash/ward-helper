node_modules/
dist/
.env
.env.local
*.log
.audit_logs/
.DS_Store

# Emitted TS artifacts (we use vite for bundling; tsc is --noEmit only)
src/**/*.js
src/**/*.js.map
tests/**/*.js
tests/**/*.js.map
*.tsbuildinfo

# Synthetic safety-engine scenario runs.
# baseline.txt is the frozen reference — diff against it on every change.
# latest.txt is the most recent local run; never committed.
scripts/scenarios/latest.txt

# 2026-05-10: chaos-bot output + test coverage (prevent git add -A near-misses)
chaos-reports/
coverage/

# 2026-05-10: relocated /remote-control session transcript — personal reference,
# not a project artifact. Lives on disk for retrieval but not tracked in repo.
docs/2026-05-10-remote-control-session.txt

# 2026-05-14: local MCP server config (no secrets, just public project refs)
# and Supabase CLI scratch dir. Personal tooling, not project state.
.mcp.json
supabase/.temp/

# 2026-05-17: PR-B2 bake-workstream local scratch. The DEVICE bake doc is
# a fill-per-device template that says "delete before commit"; the
# precision-repair script is a one-off bot-table fixer. Both are working
# scratch, not project state — ignored so a stray `git add -A` can't
# commit them. (b1-bake-witness.mjs is the opposite — spec-referenced
# infra — and is tracked, not ignored.)
docs/audit/2026-05-14-pr-b2-bake-DEVICE.md
scripts/fix-precision-v4-1.mjs
.env.production
.env.*.local
