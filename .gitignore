node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# Emitted TS artifacts (we use vite for bundling; tsc is --noEmit only)
src/**/*.js
src/**/*.js.map
tests/**/*.js
tests/**/*.js.map
*.tsbuildinfo

# Synthetic safety-engine scenario runs.
# baseline.txt is the frozen reference — diff against it on every change.
# latest.txt is the most recent local run; never committed.
scripts/scenarios/latest.txt

# 2026-05-10: chaos-bot output + test coverage (prevent git add -A near-misses)
chaos-reports/
coverage/
